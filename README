On rhel 7 change "[[ -f /usr/share/git/completion/git-prompt.sh ]] && source /usr/share/git-core/contrib/completion/git-prompt.sh;"
git clone git://github.com/jonaz/jonaz-vim-rc.git .vimdir

cd .vimdir
./install

or 

ln -s .vimdir/.vim .vim
ln -s .vimdir/.vimrc .vimrc
